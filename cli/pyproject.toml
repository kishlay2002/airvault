[project]
name = "vaultmind-cli"
version = "0.1.0"
description = "VaultMind Admin CLI"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1.0",
    "httpx>=0.27.0",
    "rich>=13.7.0",
]

[project.scripts]
vaultmind = "vaultmind.__main__:cli"
